@Library('pipeline-library@pull/940/head') _

buildDockerAndPublishImage('404', [
    targetplatforms: 'linux/amd64,linux/arm64',
    registryNamespace: 'jayfranco', // Use your Docker Hub username as the namespace
    cacheTo: 'type=registry,mode=max,ref=jayfranco/404:latest-cache'
])
